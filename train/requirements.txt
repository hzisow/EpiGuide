# Symptom model pipeline. Deliberately small: the whole thing is a logistic
# regression over 22 binary features, and adding a deep-learning framework to
# fit 23 numbers would be theatre.
numpy>=1.24
scikit-learn>=1.3

# Optional. Only needed for `train_symptom_model.py --framework both`, which
# refits the same model as a single Keras dense layer purely to confirm the two
# frameworks agree on the weights. The vision model in train/vision/ genuinely
# needs TensorFlow; see train/vision/requirements.txt.
# tensorflow>=2.15
